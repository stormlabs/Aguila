{% for task in tasks %}
<div>
    <h3><a href="{{ path('task_show', {'id': task.id}) }}">{{ task.description }}</a></h3>
    <p>
        {{ 'task.index.reported_by'|trans([], 'AguilaBundle') }} <strong>{{ task.reporter }}</strong>
        {% if task.assignee %}
        {{ 'task.index.and_assignee'|trans([], 'AguilaBundle') }} <strong>{{ task.assignee }}</strong>
        {% endif %}
    </p>
</div>
{% else %}
<div>
    {{ "task.index.no_tasks"|trans([], 'AguilaBundle') }}
</div>
{% endfor %}