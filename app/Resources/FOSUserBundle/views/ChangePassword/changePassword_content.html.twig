{% form_theme form theme %}

<form action="{{ path('fos_user_change_password') }}" {{ form_enctype(form) }} method="POST" class="form-stacked">
    <fieldset>
        {{ form_widget(form) }}
    </fieldset>
    <div class="actions">
        <input class="btn primary" type="submit" value="{{ 'change_password.submit'|trans({}, 'FOSUserBundle') }}"/>
    </div>
</form>
